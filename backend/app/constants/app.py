API_V1_PREFIX = "/api/v1"

SERVICE_NAME = "interview-ai-agent-backend"

# No real auth yet (deliberately deferred, see CLAUDE.md) - every row is
# owned by this one hardcoded user until real auth/login is built.
DEFAULT_USER_ID = "default-user"

// Placeholder — wired to real upload logic once the backend's Stage 1
// resume/JD ingestion endpoints are built (see root CLAUDE.md, Stage 1).
export function ResumeUploadPage() {
  return (
    <div className="mx-auto max-w-2xl p-8">
      <h2 className="text-xl font-semibold text-slate-900">
        Resume + job description setup
      </h2>
      <p className="mt-2 text-slate-600">
        Placeholder page. Will upload a resume PDF and a JD, matching the
        Stage 1 backend endpoints once they exist.
      </p>
    </div>
  )
}

export default ResumeUploadPage
